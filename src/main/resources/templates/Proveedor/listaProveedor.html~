<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <title>Lista Prestaciones</title>
</head>
<body>
<div class="container mt-3">
    <!-- Formulario de búsqueda -->
    <form th:action="@{/prestaciones}" method="get" class="mb-3">
        <div class="input-group">
            <input type="text" name="buscarPrestacion" class="form-control" placeholder="Buscar prestación por nombre" th:value="${buscarPrestacion}">
            <button class="btn btn-primary" type="submit">Buscar</button>
        </div>
    </form>

    <table class="table table-hover">
        <thead>
        <tr>
            <th>Código</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Precio</th>
            <th>Proveedor</th>
            <th>Opciones</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="prestacion : ${prestaciones}">
            <td th:text="${prestacion.id}"></td>
            <td th:text="${prestacion.nombre}"></td>
            <td th:text="${prestacion.descripcion}"></td>
            <td th:text="${prestacion.precio}"></td>
            <td th:text="${prestacion.proveedor != null ? prestacion.proveedor.nombre : 'Sin proveedor'}"></td>
            <td>
                <a th:href="@{/actualizarPrestacion/} + ${prestacion.id}" class="btn btn-warning">Actualizar</a>
                <a th:href="@{/eliminarPrestacion/} + ${prestacion.id}" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
        </tbody>
    </table>

    <!-- Botones de acción -->
    <a th:href="@{/formularioPrestacion}" class="btn btn-success">Nueva Prestación</a>
    <a th:href="@{/prestaciones/pdf}" class="btn btn-info">Generar PDF</a>
</div>
</body>
</html>
